# 개발 진행 기록

기능이 하나씩 완성될 때마다 이 문서에 기록을 추가합니다. 전체 기획은 [기획문서.md](기획문서.md) 참고.

## 현재 상태 요약 (새 세션에서 이어서 작업할 때 먼저 읽기)

**게임**: 탕탕특공대 — 뱀서류풍 탑다운 서바이버. `index.html` 단일 파일, 로컬 정적 서버(`.claude/launch.json`, `python -m http.server 8765`)로 미리보기.

**완성된 기능 (Step 1~26, 전부 완료)**:
- 기본 조작(WASD/방향키), 자동 공격, 적 스폰
- 경험치/레벨업/카드 선택(16종), 3D 캐릭터(three.js, 로드 실패 시 2D 자동 대체)
- 1분 중간보스 / 3분 최종보스, 최종보스 처치 시 스테이지 클리어
- 메인 메뉴 ↔ 상점 ↔ 스테이지 플레이 전체 흐름, 골드/영구 강화(`localStorage` 저장), 스테이지별 몬스터 강화 배율
- 회전 장애물, 타격감 연출(화면 흔들림·피격 플래시·파티클·자석 모션), 카드 UI 개선
- 일반 적/보스/장애물/배경 3D 렌더링, 카드 이모지 정리, 카드 효과 플로팅 텍스트 + 무기 실체화/반동/총구 화염
- (수정) 3D 배경이 2D 이펙트를 가리던 z-order 버그 수정, 일반 몬스터를 실제 MD2(ogro)/Collada(elf) 모델로 교체, 최종보스 크기/방향 수정, 실제 무기(w_blaster.md2) 모델을 캐릭터 옆에 배치 + 투사체(에너지 볼트) 비주얼 개선
- 오크 몬스터 방향 재보정, 참고 이미지 스타일의 던전 배경(돌바닥/테두리/붉은 크리스탈 기둥/보랏빛 조명), Web Audio API로 합성한 효과음(공격/피격/처치/카드선택/보스등장/클리어/게임오버) + 루프 배경음악
- 사용자가 직접 제공한 실제 이미지(`assets/`)를 카드/장애물/배경/타이틀 화면에 적용, 효과음을 더 어둡고 무섭게 재조정 + 메인메뉴/상점 앰비언스·클릭 효과음 추가
- 게임 분위기에 맞춘 UI 전면 개편: 게임 폰트(Black Han Sans/Do Hyeon), 자체 제작 CSS 버튼(석재 플레이트+젬), 상점 UI 전면 재구성(영구 강화 아트+레벨 핍+금색 가격 플레이트), 조작법 접이식 처리, 장애물 회전 제거(고정), 경험치 드랍을 보랏빛 마력 결정으로 교체, 공포 톤 BGM(초저음 드론+심장박동+트라이톤)
- 장애물 흙 얼룩 오프라인 제거(`obstacles_cut.png`) + 경기장 내부로만 배치, 실측한 경기장 좌표로 플레이어·적 이동 범위 제한, 게임 중 HUD 다시 시작/메인으로 버튼, 상점 8종으로 확장(모든 항목에 현재 누적 효과 문구 + 최대 레벨 상한), 레벨업 화면에 현재 능력치·보유 카드 효과 요약 패널, 던전 배경을 CSS로 전환하고 장애물을 2D 소품 캔버스로 옮겨 `file://`로 직접 열어도 정상 동작하도록 수정
- 스테이지별 난이도(적 스탯 배율/스폰 속도) 강화, 중간·최종 보스 전용 공격 스킬(각 2종, 텔레그래프+발동 연출) 추가 + 화면 상단 보스 체력바, 시작 화면 전용 BGM 작곡, 상점에 구매/장착 가능한 캐릭터 스킨 5종 추가
- 폭발탄 카드의 스플래시 피해를 확장 충격파 링+화염 파티클+화면 흔들림+전용 폭발음으로 명확하게 표현, 최종 보스 체력/공격력/스킬 재사용 속도 상향
- **(신규)** 폭발탄 카드의 화면 흔들림이 공격 속도가 빠를 때 거의 끊이지 않고 계속되던 문제 수정(흔들림 제거, 링/파티클/효과음은 유지)

**이 프로젝트에서 지켜온 작업 방식** (다음 세션에서도 동일하게 진행):
1. 기능을 작게 나눠 하나씩 구현
2. `window.__debug`에 내부 함수/상태를 임시로 노출해 결정론적으로 검증(예: `game.paused=true`로 실시간 진행을 멈추고 수동으로 `update()` 호출) → 검증 후 반드시 디버그 코드 제거
3. 브라우저 미리보기로 스크린샷 확인까지 마친 뒤 완료 처리
4. 기능이 끝날 때마다 이 문서(PROGRESS.md)에 구현 내용·검증 방법·주의사항을 기록
5. 재시작/화면 전환 버튼은 항상 중복 클릭 가드 적용, 새 버튼을 추가할 때도 동일하게 적용

**알아두면 좋은 것**:
- Step 21부터는 예외적으로 사용자가 직접 만들어 제공한 이미지(`assets/` 폴더, ChatGPT로 직접 생성한 이 프로젝트 전용 아트)를 그대로 사용함 — 이는 실제 상용 "탕탕특공대" 게임 에셋을 가져온 것이 아니라 사용자 소유의 커스텀 아트이므로, 기존에 지켜온 "실제 저작물 재현 금지" 원칙과 충돌하지 않음
- 실제 "탕탕특공대" 게임의 이미지·에셋은 저작권상 쓰지 않고 항상 자체 도형/오픈소스 에셋(three.js 예제 모델)으로 재해석함
- three.js 애드온은 import map(`"three"` bare specifier)이 없으면 조용히 전체 모듈이 로드 실패하니 주의
- 브라우저 미리보기 탭이 background/hidden 상태가 되면 `requestAnimationFrame`이 멈춰서 실시간 진행이 안 보일 수 있음 → 이럴 때는 디버그 훅으로 직접 프레임을 호출해서 검증

## 단계별 상세 기록

---

## Step 1. 기본 조작 (완료)

- **구현 내용**
  - 900×600 캔버스 아레나, 그리드 배경
  - 플레이어(파란 원)를 캔버스 중앙에서 시작
  - `W A S D` / 방향키로 8방향 이동, 대각선 이동 시 속도 정규화(과속 방지)
  - 화면 경계 클램핑으로 캔버스 밖으로 나가지 않음
  - 시작 오버레이(조작법 안내) + 시작하기 버튼
  - 재시작 버튼 가드 플래그 + `requestAnimationFrame` 정리 로직 선반영 (더블클릭 대비)
- **검증 방법**: 임시 디버그 훅(`window.__debug`)으로 `update()`를 직접 호출해 우/하/대각선 이동량과 경계 클램핑 값을 확인 후 디버그 코드 제거
- **파일**: `index.html`, `.claude/launch.json`(로컬 미리보기용 정적 서버 설정 추가)

## Step 2. 적 스폰 + 자동 공격 (완료)

- **구현 내용**
  - 적(빨간 원)이 화면 가장자리 랜덤 위치에서 주기적으로 스폰 (스폰 간격은 시간 경과에 따라 1400ms → 최소 450ms까지 점점 짧아짐)
  - 적은 항상 플레이어를 향해 직선으로 추격
  - 플레이어는 사정거리(240px) 안의 가장 가까운 적을 0.5초마다 자동으로 공격 (투사체 발사, 데미지 12)
  - 적과 접촉하면 0.7초마다 플레이어 체력 8 감소 (접촉 데미지)
  - 적 처치 시 처치 수 +1, 점수 +10, HUD에 실시간 반영
  - 플레이어 체력이 0 이하가 되면 게임 오버 → 종료 오버레이(최종 점수 표시) + "다시 시작" 버튼
- **검증 방법**: 디버그 훅으로 다음 시나리오를 직접 실행/확인 후 코드 제거
  - 사정거리 안의 적을 자동 공격 → 처치 → 킬 카운트/점수 증가
  - 접촉 중인 적으로부터 체력이 주기적으로 깎이는지 확인
  - 체력 0 도달 시 `gameOver`/`running` 상태 전환과 종료 오버레이 표시 확인
  - 종료 화면에서 재시작 버튼 빠르게 두 번 클릭해도 정상적으로 한 번만 초기화되는지 확인
  - 브라우저 미리보기(`http://localhost:8765`)로 실제 화면에서 적 스폰/추격/피격/처치가 정상 표시되는지 스크린샷으로 최종 확인
- **파일**: `index.html`

## Step 3. 경험치 / 레벨업 / 카드 선택 (완료)

- **구현 내용**
  - 적 처치 시 경험치 구슬(초록 원, 경험치 8) 드랍, 플레이어가 일정 거리 안에 들어오면 자동 획득
  - 레벨업 요구 경험치는 `20 + (레벨-1) * 15`로 증가
  - 레벨업 시 게임을 일시정지(`game.paused`)하고 카드 3장(중복 없이 랜덤)을 보여주는 오버레이 표시
  - 카드 5종: 공격력 +5 / 공격 속도 +15% / 이동 속도 +12% / 최대 체력 +20(즉시 회복) / 사정거리 +40
  - 카드 선택 시 해당 효과가 `game.player` 스탯에 반영되고 게임 재개
  - HUD에 레벨(Lv.)과 경험치 바 추가
- **검증 방법**: 디버그 훅으로 다음을 격리된 상태(`game.paused=true`로 실시간 진행 차단)에서 직접 확인 후 코드 제거
  - `gainExp()` 호출 시 임계치 도달 여부에 따라 레벨업이 정확히 트리거되는지
  - 레벨업 시 `paused=true` + 카드 3장 렌더링 확인
  - 카드 클릭 시 해당 스탯이 정확한 수식대로 반영되고 `paused=false`로 복귀하는지
  - 카드 5종 각각의 `apply()` 함수가 올바른 계산 결과를 내는지 개별 확인
  - 브라우저 미리보기로 HUD의 Lv./경험치 바 렌더링 확인
- **주의사항 / 알게 된 점**: 브라우저 미리보기 탭이 백그라운드로 전환되면(`document.hidden`) `requestAnimationFrame`이 브라우저에 의해 멈춰서 실시간 진행이 안 보일 수 있음. 이 경우 로직 자체는 `window.__debug`로 직접 `update()`를 호출해 검증했음 (테스트 후 디버그 코드는 매번 제거함)
- **파일**: `index.html`

## Step 4. 생존 타이머 + 승리 조건 + 점수 공식 완성 (완료)

- **구현 내용**
  - 5분(300,000ms) 카운트다운 타이머를 HUD에 실시간 표시 (`formatTime()`으로 mm:ss 포맷, 레벨업 일시정지 중에는 멈춤)
  - 생존 시간이 5분에 도달하면 승리 처리 (`endGame(true)`), 체력 0이면 패배 처리(`endGame(false)`) — 하나의 `endGame(win)` 함수로 통합
  - 최종 점수 공식 완성: `처치 점수(처치당 10) + 생존 시간 보너스(초당 2점) + 레벨 보너스(레벨당 20점)`
  - 종료 화면에 처치 수 / 레벨 / 생존 시간 요약 문구 추가
  - 시작 화면 조작법에 "5분 생존 시 승리" 규칙 추가
- **검증 방법**: 디버그 훅으로 `game.paused=true`로 실시간 진행을 멈춘 뒤 결정론적으로 확인 후 코드 제거
  - `formatTime()` 경계값(0, 5분 정각, 90초) 결과 확인
  - 생존 시간이 임계값을 막 넘는 순간 승리 처리(`gameOver`/`running` 전환, 타이틀/점수/설명 텍스트) 확인
  - 체력이 0 밑으로 떨어지는 순간 패배 처리 및 동일한 점수 공식이 적용되는지 확인 (직접 계산값과 대조)
  - 승리·패배 종료 화면에서도 재시작 버튼 빠른 두 번 클릭이 안전한지 재확인
  - 브라우저 미리보기로 시작 화면 규칙 텍스트 렌더링 확인
- **파일**: `index.html`

## Step 5. 보스전 개편 + 밸런스/레이아웃 수정 (완료)

- **구현 내용**
  - 게임 제목을 "탕탕특공대"로 변경 (`<title>`, H1, 종료 화면 관련 텍스트, footer)
  - 생존 타이머 5분 → **3분**으로 단축
  - 승리 조건 변경: 기존 "시간 생존 시 자동 승리" 방식을 폐지하고, **1분에 중간 보스, 3분에 최종 보스**가 등장 → **최종 보스를 처치해야 클리어**되는 방식으로 전환
  - 보스는 일반 몹과 다른 타입(`mid`/`final`)으로 관리, 각각 체력/이동속도/접촉 데미지/처치 보상(점수·경험치)이 별도로 설정됨 (중간 보스 HP 400, 최종 보스 HP 900). 테두리 강조선으로 일반 몹과 시각적으로 구분
  - 보스 등장 시 화면 상단에 "⚠ OO 보스 출현!" 배너 표시
  - 기본 공격 사거리를 240px → **150px**로 축소해 근접 포지셔닝의 중요도를 높임
  - **레이아웃 버그 수정**: 캔버스가 좁은 화면(반응형)에서 부모 컨테이너보다 커서 오른쪽이 잘려 보이던 문제 → `canvas{max-width:100%;height:auto;}` 추가로 비율 유지 축소되도록 수정. 데스크톱 폭 계산 오차도 함께 점검 (`#app` 너비를 캔버스+테두리+패딩 합에 맞게 940px로 조정)
- **검증 방법**: 디버그 훅으로 다음을 확인 후 코드 제거
  - 1분/3분 시점에 정확히 중간/최종 보스가 스폰되는지 (`midBossSpawned`/`finalBossSpawned` 플래그와 생성된 적의 `type`, `hp` 확인)
  - 최종 보스의 HP를 0으로 만들었을 때 다음 프레임에 승리 처리(`gameOver`, 종료 타이틀 "🎉 최종 보스 처치! 클리어!") 확인
  - 좁은 뷰포트에서 캔버스 렌더링 크기가 컨테이너 폭 안에 들어오고 문서 레벨 가로 스크롤이 발생하지 않는지 확인
  - 브라우저 미리보기로 제목/타이머/규칙 텍스트가 화면 밖으로 잘리지 않고 정상 표시되는지 스크린샷 확인
- **파일**: `index.html`

## Step 6. 레벨업 카드 다양화 (완료)

- **구현 내용**: 기존 5종에 7종을 추가해 총 12종으로 확장. 새 스탯은 모두 `game.player`에 기본값(효과 없음 상태)으로 추가되어 있음
  - 다중 사격(`projectileCount`): 공격 시 사거리 내 가장 가까운 적 N마리를 동시에 조준해 화살 발사
  - 관통 화살(`pierce`): 화살이 맞은 뒤에도 사라지지 않고 추가로 N마리까지 관통
  - 흡혈(`lifesteal`): 화살로 적을 처치하면 체력 회복
  - 자석(`pickupRadius`): 경험치 구슬 획득 판정 범위 증가
  - 치명타(`critChance`): 일정 확률로 2배 피해 (최대 60%로 상한)
  - 재생(`regen`): 초당 체력 자동 회복
  - 가시 갑옷(`thorns`): 적이 접촉 공격할 때마다 그 적에게도 반사 피해
- **검증 방법**: 디버그 훅으로 다음을 확인 후 코드 제거
  - 카드 12종 전체를 순서대로 적용했을 때 각 스탯이 정확한 수식대로 누적되는지
  - 다중 사격: `projectileCount=2`일 때 사거리 내 적이 3마리 있어도 정확히 가까운 2마리만 조준해 화살 2발이 생성되는지
  - 관통: 관통 남은 횟수가 있는 화살은 적 처치 후에도 사라지지 않고 `pierceLeft`만 감소하는지
  - 흡혈: 화살에 맞아 적이 죽는 순간에만 체력 회복이 발생하는지
  - 치명타: `Math.random()`을 강제로 극단값으로 두고 크리티컬 유/무에 따라 정확히 2배 피해가 들어가는지
  - 재생/가시: 다른 이펙트(자동공격 등)와 간섭 없이 격리한 상태에서 독립적으로 수치가 맞는지
  - 자석: 기존 반경 밖·확장 반경 안에 있는 구슬이 정상적으로 흡수되는지
- **파일**: `index.html`

## Step 7. 비주얼 개선 + 배경화면 (완료)

- **구현 내용**
  - **주의**: 실제 "탕탕특공대" 게임의 이미지·에셋은 저작권 문제로 그대로 가져오지 않았고, 같은 느낌(치비 스타일 탑다운 슈터)을 자체 도형으로 재해석해서 구현함
  - 캔버스 배경을 단순 격자선에서 **타일링된 지면 텍스처**(오프스크린 캔버스로 만든 패턴, 은은한 그라데이션 + 노이즈 점)로 교체
  - 플레이어를 단순 원에서 **총(건 배럴) + 얼굴(눈)이 있는 캐릭터**로 개선. 이동 방향에 따라 총구가 회전하며 향함
  - 일반 몹에 **눈** 추가로 캐릭터성 부여
  - 보스(중간/최종)는 **가시 왕관 테두리**로 일반 몹과 명확히 구분되도록 표현
  - 투사체를 단순 점에서 **탄환 + 잔상(트레이서 라인)**으로 개선
- **검증 방법**: 브라우저 미리보기로 실제 렌더링 확인 (배경 패턴, 캐릭터/적 도형, 총구 방향, 콘솔 에러 없음), 변경 후에도 재시작 더블클릭 등 기존 흐름이 깨지지 않았는지 재확인
- **파일**: `index.html`

## Step 8. 회전 장애물 + 메인 메뉴/상점/스테이지 진행 시스템 (완료)

- **구현 내용**
  - **회전 장애물**: 스테이지 시작 시 5개의 육각형 장애물을 겹치지 않게(플레이어 시작 위치와도 일정 거리 이상) 랜덤 배치. 각 장애물은 제자리에서 계속 회전(시각 효과)하며, 플레이어·적 모두 통과할 수 없고 부딪히면 장애물 밖으로 밀려남 (투사체는 장애물을 무시하고 통과 — 의도적 단순화)
  - **화면 흐름 개편**: 기존 "시작하기 → 플레이 → 재시작"의 단순 루프를 메인 메뉴 중심 구조로 전면 개편
    - **메인 메뉴**: 조작법 안내 + 보유 골드/현재 스테이지 표시 + "스테이지 N 시작" / "상점" 버튼
    - **스테이지 클리어 화면**(최종 보스 처치 성공 시): 최종 점수·획득 골드 표시 + "상점" / "다음 스테이지" / "게임 종료(메인 화면)" 3버튼
    - **게임 오버 화면**(사망 시): 점수·생존 시간·획득 골드 표시 + "다시 시도"(같은 스테이지 재도전) / "메인 화면" 버튼
    - **상점**: 골드로 영구 강화 구매(체력/공격력/이동속도/자석 범위), 구매할 때마다 다음 레벨 비용이 1.5배씩 상승. 구매 내역은 `localStorage`에 저장되어 다음 방문·다음 스테이지에도 유지됨
  - **점점 강해지는 몬스터**: 스테이지 번호에 따라 일반 몹·보스의 체력/접촉 데미지/보상(점수·경험치)이 `1 + (스테이지-1)×0.3` 배율로 스케일링. 사망(패배) 시에는 스테이지가 오르지 않고, 클리어(승리) 시에만 다음 스테이지로 진행
  - 골드는 승패 관계없이 그 판의 최종 점수의 50%를 지급 (재도전을 유도)
  - 모든 화면 전환/구매 버튼에 기존과 동일한 방식의 중복 클릭 가드 적용
- **검증 방법**: 디버그 훅으로 다음을 확인 후 코드 제거
  - 장애물이 플레이어 시작 지점과 겹치지 않게 5개 생성되는지, 플레이어를 장애물 위치로 강제 이동시켰을 때 다음 프레임에 정확히 경계 밖(반지름 합)으로 밀려나는지
  - 같은 조건에서 스테이지 1과 스테이지 3의 스폰된 일반 몹 체력/데미지/보상이 정확한 배율로 다른지
  - 최종 보스 처치 시: 점수 계산, 골드 지급, `meta.stage` 증가, 스테이지 클리어 화면 텍스트, `localStorage` 저장까지 한 번에 확인
  - 사망 시: 골드는 지급되지만 `meta.stage`는 그대로인지, 종료 화면 텍스트가 올바른지
  - 상점 구매 시 골드 차감·강화 레벨 증가·다음 비용 재계산이 정확한지, 구매 후 시작한 새 스테이지에 강화 효과(예: 최대 체력 증가)가 실제로 반영되는지
  - "다음 스테이지"/"다시 시도"/상점 구매 버튼 등 신규 버튼들의 중복 클릭 안전성
  - 브라우저 미리보기로 메인 메뉴 → 상점 → 스테이지 플레이(장애물 렌더링 확인)까지 실제 화면 흐름을 눈으로 확인
- **주의사항**: 실제 "탕탕특공대"의 이미지·에셋은 여전히 사용하지 않음(자체 도형 재해석 유지). 진행 상황은 브라우저의 `localStorage`에 저장되므로, 브라우저 데이터를 지우거나 다른 브라우저/시크릿 모드로 열면 골드·강화 진행이 초기화됨
- **파일**: `index.html`

## Step 9. 타격감 연출 + 카드 개선/확장 (완료)

- **구현 내용**
  - **자석 끌어당기는 모션**: 경험치 구슬이 `pickupRadius` 안에 들어오면 즉시 흡수되지 않고, 플레이어에게 점점 빨라지며 끌려오다가 아주 가까워지면(플레이어 반경) 흡수됨
  - **피격 연출**: 적과 접촉해 피해를 입을 때마다 (1) 화면이 짧게 흔들리고(damage 비례 강도) (2) 화면 전체에 붉은 플래시가 번쩍였다 사라지고 (3) 플레이어 위치에서 붉은 파티클(피 튀김)이 튐
  - **적 처치 모션**: 적이 죽으면 즉시 사라지는 대신, 그 자리에서 자신의 색으로 파티클이 터지고 잔상이 작아지며 페이드아웃(보스는 더 크고 화려하게)
  - **카드 비주얼 개선**: 카드마다 이모지 아이콘 + 테마 컬러(상단 강조선·제목 색) 추가, 호버 시 살짝 떠오르며 그림자가 생기는 애니메이션 적용
  - **카드 4종 신규 추가**(기존 12종 → 16종): 폭발탄(💣 명중 시 주변 적에게 스플래시 피해), 냉기 사격(❄️ 명중한 적 이동속도 감소), 황금손(💰 스테이지 종료 골드 +20%), 여벌 목숨(💗 치명적 피해 1회 무효화 후 체력 30%로 부활)
- **검증 방법**: 디버그 훅으로 다음을 확인 후 코드 제거
  - 구슬이 즉시 사라지지 않고 여러 프레임에 걸쳐 플레이어 쪽으로 이동하다가 최종적으로 흡수되는지 (실제 프레임 간격 16ms 기준)
  - 접촉 피해 발생 시 `shakeTime`/`hitFlash`/파티클 개수가 정확히 세팅되는지, 적 처치 시 `deathFx`와 파티클이 생성되는지
  - 폭발탄: 직접 맞은 적은 원래 피해, 스플래시 반경 안의 다른 적은 스플래시 피해만 받는지
  - 냉기 사격: 슬로우가 걸린 적의 이동 거리가 감속 배율대로 정확히 줄어드는지 (테스트 중 큰 dt로 인한 타이머 감산 순서 버그를 발견해 수정함 — 슬로우 배율을 먼저 계산한 뒤 타이머를 감산하도록 순서 교정)
  - 여벌 목숨: 첫 치명타는 체력 30%로 부활 처리되고 소진 후 두 번째 치명타는 정상적으로 게임 오버 처리되는지
  - 황금손: 스테이지 종료 골드 계산에 보너스 배율이 정확히 반영되는지
  - 새 카드 16종 전체를 순서대로 적용했을 때 모든 스탯이 예상한 수식대로 누적되는지
  - 브라우저 미리보기로 카드 UI(아이콘/색상)와 피격 플래시가 실제로 보이는지 스크린샷 확인
- **파일**: `index.html`

## Step 10. three.js 3D 캐릭터 적용 (완료, 실험적)

- **구현 내용**
  - 참고 링크(https://threejs.org/examples/#webgl_animation_skinning_blending)의 three.js 공식 예제 방식을 그대로 가져와서, 같은 예제에서 쓰이는 리깅된 군인 3D 모델(`Soldier.glb`, three.js 공식 저장소의 예제 에셋 — CC0/오픈소스 데모 자산)을 로드해 플레이어 캐릭터에 적용
  - CDN(jsDelivr)에서 three.js 모듈과 GLTFLoader를 불러오도록 `<script type="module">` + `<script type="importmap">`로 전환 (three.js 애드온 모듈이 내부적으로 `"three"`라는 bare import를 사용하기 때문에 import map이 반드시 필요함 — 처음엔 이걸 몰라서 모듈 전체가 조용히 로드 실패하는 문제를 겪었고, 원인을 찾아 수정함)
  - 기존 2D 캔버스 위에 투명한 `#three-canvas`(WebGL)를 겹쳐서, 탑다운에서 살짝 기울어진(68°) 직교 카메라로 캐릭터만 렌더링. 나머지(적, 장애물, 이펙트 등)는 기존 2D 캔버스 그대로 유지
  - 게임 좌표(`game.player.x/y`, 픽셀)를 three.js 월드 좌표로 변환하는 스케일(`WORLD_SCALE`)을 계산해서 2D 판정(사정거리 링 등)과 3D 모델 위치가 어긋나지 않도록 맞춤
  - 이동 시 Idle ↔ Walk 애니메이션을 부드럽게 크로스페이드(가중치 블렌딩)로 전환 — 원본 예제의 "animation blending" 컨셉을 그대로 적용
  - **로드 실패 시 자동 대체**: 네트워크 문제 등으로 3D 모델을 불러오지 못하면 예외를 잡아 기존 2D 치비 캐릭터(총 든 원+눈) 렌더링으로 자동 전환되어 게임이 깨지지 않음
- **검증 방법**
  - 브라우저 콘솔/네트워크로 실제 three.js·GLTFLoader·Soldier.glb 요청이 성공하는지 확인 (처음엔 import map 누락으로 전체 모듈이 조용히 실행되지 않는 문제를 발견 → 수정 후 재확인)
  - 디버그 훅으로 플레이어를 화면 중앙/좌상단/우하단 등 여러 위치로 강제 이동시키고 스크린샷으로 3D 캐릭터가 2D 사정거리 링 중심과 정확히 겹치는지 확인
  - 이동 키를 누르고 있는 동안 `walkWeight`(애니메이션 블렌딩 가중치)가 0→1로, 떼면 다시 0으로 자연스럽게 돌아오는지 수치로 확인 + 스크린샷으로 실제 걷는 포즈 변화 확인
  - 실제 메인 메뉴 → 스테이지 시작 버튼 클릭까지 진짜 UI 플로우로 재생해 3D 캐릭터가 정상 표시되는지 최종 확인
- **주의사항 / 한계**
  - 실험적 기능입니다: 외부 CDN(jsDelivr)에 의존하므로 **인터넷 연결이 없으면 3D 캐릭터 대신 기존 2D 캐릭터로 자동 대체**됩니다 (배포하지 않는 개인용 프로젝트라는 전제하에 CDN 의존을 그대로 사용함)
  - 카메라에 약간의 기울기(68°)가 있어 완벽한 정사영은 아니며, 화면 가장자리에서 아주 미세한 시각적 오차가 있을 수 있음 (여러 위치에서 스크린샷으로 확인한 결과 눈에 띄는 수준은 아니었음)
  - 현재는 Idle/Walk 두 애니메이션만 사용 (모델에는 Run 등 다른 클립도 있음 — 필요하면 추후 추가 가능)
  - 적/보스는 여전히 2D로 남아있음 (요청 범위가 "캐릭터"였기 때문에 플레이어에만 적용)
- **파일**: `index.html`

## Step 11. 적/보스/장애물/배경 3D화 (three.js) (완료)

- **구현 내용**
  - **일반 적**: `THREE.InstancedMesh`(IcosahedronGeometry, 최대 300개) 하나로 렌더링해 다수 스폰에도 가볍게 처리. 개별 인스턴스마다 살짝 흔들리는 회전/부양 애니메이션(`en._spin`)을 부여해 밋밋한 도형이 아니라 살아있는 느낌을 줌
  - **보스(중간/최종)**: three.js 공식 예제 자산인 `RobotExpressive.glb`(CC0, mrdoob/three.js 저장소)를 지연 로드(첫 보스 등장 시점에 1회 로드, 이후 재사용)해서 적용. 재질을 복제한 뒤 색상만 보스 타입별(`BOSS_TYPES.color`)로 다르게 입혀 중간 보스/최종 보스를 구분. 스킨드 메시라서 `SkeletonUtils.clone()`으로 복제(일반 `Object3D.clone()`은 스켈레톤 바인딩이 깨짐), Idle 애니메이션을 반복 재생. 보스가 여럿 동시에 존재할 수 있는 엣지 케이스도 `Map`으로 개별 관리
  - **회전 장애물**: 기존 2D 육각형 대신 `OctahedronGeometry` 크리스탈 메시로, 스테이지 시작(`rebuildObstacles3D()`)마다 `game.obstacles` 배치에 맞춰 새로 생성. 기존 회전 각도(`ob.angle`)를 그대로 3D 회전에 동기화
  - **배경(지면)**: 기존 2D 타일 패턴을 그리던 오프스크린 캔버스를 `THREE.CanvasTexture`로 재사용해 3D 바닥 평면에 반복 적용 — 새 이미지 에셋 없이 기존 비주얼 톤을 3D로 확장
  - 3D 표현이 준비된 개체는 2D 캔버스에서 몸통(원/스파이크 크라운/눈)을 더 이상 그리지 않고, HP바·냉기 슬로우 테두리 같은 상태 표시만 2D로 얹음(`en._has3D` 플래그로 프레임마다 판정). 3D 로드에 실패해도(네트워크 문제 등) 해당 개체는 자동으로 2D로 폴백되어 안 보이는 일이 없음
  - 게임 재시작 시 이전 판의 보스 3D 객체/재질을 정리(dispose)하고 장애물 메시를 다시 생성하도록 `startNewGame()`을 보강
- **검증 방법**
  - 브라우저 콘솔 에러 없음을 확인 (three.js/GLTFLoader/RobotExpressive.glb 로드 포함)
  - 디버그 훅(`window.__debug`)으로 `game.paused=true` 상태에서 중간/최종 보스를 강제 스폰하고 `updateThree()`를 반복 호출 → `en._has3D`가 `true`로 바뀌는지, `three3d.bossTemplate`이 로드되는지 확인 후 스크린샷으로 중간 보스(보라)/최종 보스(주황, 실제로 캡처됨)가 각각 다른 색의 3D 로봇으로 렌더링되는지 확인
  - 실제 플레이(자동 진행)로 일반 적이 빨간 3D 다면체로, 장애물이 갈색 크리스탈로 표시되는지 스크린샷 확인, 처치 시 정상적으로 사라지는지 확인
  - 검증 후 디버그 코드는 제거함
- **주의사항**: `RobotExpressive.glb`도 Soldier.glb와 마찬가지로 CDN(jsDelivr를 통한 three.js 공식 저장소 자산) 의존이라 오프라인이면 보스가 2D 스파이크 크라운으로 자동 대체됨. 일반 적/장애물/배경은 네트워크 없이도(로컬 지오메트리라) 항상 3D로 표시됨
- **파일**: `index.html`

## Step 12. 카드 이모지 정리 (완료)

- **구현 내용**: 카드 의미와 맞지 않거나 다른 카드와 겹쳐 헷갈리던 이모지 4종 교체
  - 긴 팔(사정거리): 🎯 → 🔭 (원거리를 더 잘 나타내는 망원경으로, 관통 카드와의 아이콘 중복도 해소)
  - 관통 화살: ➡️ → 🔱 (밋밋한 화살표 대신 꿰뚫는 느낌의 삼지창)
  - 가시 갑옷: 🛡️ → 🦔 (방패보다 "가시"를 직접 연상시키는 고슴도치)
  - 여벌 목숨: 💗 → 🩹 (튼튼한 심장 카드의 하트와 겹치지 않으면서 "치명상을 버텨낸다"는 의미에 더 맞는 반창고)
- **검증 방법**: 코드 값만 바뀌는 단순 데이터 수정이라 별도 로직 검증 없이, 브라우저에서 레벨업 카드 UI에 새 이모지가 정상 표시되는지만 확인
- **파일**: `index.html`

## Step 13. 카드 효과 시각 피드백 + 무기 실체화 (완료)

- **구현 내용**
  - **카드 습득 피드백**: 레벨업 카드를 고르면 그 즉시 캐릭터 위에 `아이콘 + 카드 이름`이 떠오르는 플로팅 텍스트, 카드 색상의 확산 파동 링, 파티클 버스트가 동시에 나와서 "뭘 먹었는지" 바로 알 수 있게 함 (카드 16종 전부 공통으로 적용되는 범용 피드백이라 개별 카드마다 놓치는 경우가 없음)
  - **무기 실체화**: 플레이어 총이 `projectileCount`(다중 사격 카드) 값만큼 총열이 늘어나 최대 4정까지 부채꼴로 늘어선 모습으로 보임 — 카드 효과가 스탯 숫자가 아니라 눈에 보이는 무기 개수 변화로 드러남. 3D 소울저 모델 위에도, 2D 폴백 캐릭터 위에도 공통으로 그려짐(three-canvas 바깥으로 총열이 뻗어 나오는 구조라 캐릭터 실루엣에 가려지지 않음)
  - **공격 모션**: 공격이 나갈 때마다 `game.shotFx`(0→1 감쇠)를 세팅해 총열이 짧게 반동(뒤로 밀렸다 복귀)하고 총구에 노란 화염 이펙트가 번쩍임
  - **치명타/흡혈/스플래시/냉기/부활 시각화**: 치명타 시 적 위에 "CRIT!" 텍스트, 흡혈 성공 시 플레이어 위에 "+N" 텍스트, 폭발탄(스플래시) 명중 시 주황 파티클 버스트, 냉기 사격에 걸린 적은 하늘색 테두리로 슬로우 상태 표시, 여벌 목숨 발동 시 "부활!" 텍스트 추가
- **검증 방법**
  - 디버그 훅으로 `game.paused=true` 상태에서 다중 사격 카드를 2회 강제 적용(`projectileCount=3`) 후 `draw()`를 직접 호출해, 캔버스의 플레이어 위쪽(플로팅 텍스트 영역)과 앞쪽(총구 영역) 픽셀을 `getImageData`로 샘플링 → 배경(무채색 타일)과 확연히 다른 유채색 픽셀이 다수 검출되어 텍스트/총열/화염이 실제로 그려지고 있음을 수치로 확인
  - `CARD_POOL`에서 카드를 직접 찾아 적용해보며 `game.player.projectileCount` 등 스탯이 정확히 반영되는지, `game.floatTexts`/`game.buffPulses` 배열에 항목이 쌓이는지 확인
  - 브라우저 콘솔에 에러가 없는지 확인
  - 검증 후 디버그 코드는 제거함
- **주의사항**: 이번 세션에서는 브라우저 미리보기 탭이 다른 세션과 공유되어(포트 8765 충돌 경고) 실시간 스크린샷 캡처가 자주 다른 진행 상태로 덮어써지는 문제가 있었음 — 콘솔 에러 확인 + 픽셀 데이터 직접 검사로 우회 검증함. 사용자가 직접 브라우저에서 카드를 먹어보며 최종 확인하는 것을 권장
- **파일**: `index.html`

## Step 14. 3D 배경이 2D 이펙트를 가리는 버그 수정 (완료)

- **문제**: Step 11에서 3D 지면(배경)을 추가한 뒤로, 피격 시 화면 흔들림/붉은 플래시/파티클, 공격 시 총구 화염/카드 습득 플로팅 텍스트 등 **2D 이펙트가 전혀 보이지 않는다**는 문제가 접수됨
- **원인**: `#three-canvas`(3D)가 `#game`(2D) 위에 항상 그려지도록 되어 있었는데, `draw()`의 "tiled ground background" 채우기(`ctx.fillRect`)에 `three3d.sceneReady` 가드를 빼먹어서 매 프레임 불투명한 2D 배경이 전체를 덮어씀 → 정작 화면에 보이는 건 그 위에 그려지는 3D 레이어뿐이었고, 2D 캔버스에 그려지는 파티클/플래시/총구화염/플로팅텍스트는 다시 그 3D 레이어에 가려져 실제로는 "2D 배경 위에서만 존재하고 화면엔 안 보이는" 상태였음
- **수정**:
  1. `#field-wrap`에 `aspect-ratio`를 주고 `#game`/`#three-canvas` 둘 다 `position:absolute`로 전환, `#game`을 `z-index:2`(위), `#three-canvas`를 `z-index:1`(아래)로 재배치 — 이제 3D는 배경, 2D는 항상 그 위에 그려지는 이펙트 레이어가 됨
  2. `draw()`의 지면 채우기(`getGroundPattern()` fillRect)에 `if (!three3d.sceneReady)` 가드 추가 — 3D 지면이 있을 때는 2D 배경을 그리지 않아 3D 레이어가 가려지지 않도록 함
  3. `.overlay`(z-index:5), `#boss-banner`(z-index:4)에 명시적 z-index를 줘서 캔버스 레이어 순서 변경에도 항상 최상단에 남도록 함
- **검증 방법**: 브라우저에서 실제 플레이 중 투사체·경험치 구슬이 3D 배경 위에 정상적으로 보이는지 스크린샷 확인, 콘솔 에러 없음 확인
- **파일**: `index.html`

## Step 15. 일반 몬스터를 실제 MD2/Collada 모델로 교체 (완료)

- **구현 내용**: 기존 InstancedMesh 아이코사헤드론(도형)을 걷어내고, 요청받은 대로 three.js 공식 예제 두 곳에서 쓰는 실제 에셋을 그대로 적용
  - **ogro** (`examples/models/md2/ogro/ogro.md2`, `webgl_loader_md2` 예제 자산, 스킨 `ctf_r.png`): `MD2Loader`로 로드한 버텍스 모프 애니메이션 지오그래피를 스폰마다 `new THREE.Mesh(공유 geometry, 공유 material)` + 개별 `AnimationMixer`로 재생("run" 클립)
  - **elf** (`examples/models/collada/elf/elf.dae`, `webgl_loader_collada`/skinning 계열 예제 자산): `ColladaLoader`로 1회 로드 후 `SkeletonUtils.clone()`으로 스폰마다 복제. 이 모델은 자체 애니메이션 클립이 없어(`collada.scene.animations.length === 0`) 정지 포즈로 표시됨(1차 적용, 추후 필요시 자체 본 회전 등으로 보완 가능)
  - 적 스폰 시(`spawnEnemy()`) `_model`을 50/50 확률로 `"ogro"`/`"elf"`로 배정
  - 개별 모델+믹서를 쓰는 특성상 InstancedMesh처럼 무제한 렌더링은 못 하므로 `ENEMY_MODEL_CAP=48`로 동시 렌더 개체 수를 제한, 초과분은 기존 2D 폴백(원+눈)으로 자연스럽게 대체(`en._has3D` 플래그로 판정)
  - 참고: 원래 `webgl_loader_collada_skinning` 예제가 쓰는 모델은 `stormtrooper.dae`이지만, 스타워즈 스톰트루퍼는 잘 알려진 저작권 캐릭터 디자인이라 이 프로젝트가 지켜온 "실제 저작물 재현 금지" 원칙에 따라 **같은 저장소의 비-IP 캐릭터인 `elf.dae`로 대체**함 (사용자에게 별도 확인 없이 기존 원칙에 따라 자체 판단, 최종 요약에서 공지)
- **검증 방법**
  - 콘솔 에러 없음 확인 (MD2Loader/ColladaLoader 로드 포함)
  - 디버그 훅으로 `three3d.ogroTemplate`/`elfTemplate` 로드 완료 여부와 `collada.scene.animations.length`(0으로 확인됨) 체크
  - 두 몬스터를 각각 강제 스폰해 스크린샷으로 실제로 다른 모양(붉은 오우거 / 녹색 엘프)으로 렌더링되는지 확인
  - **스케일 버그 발견 및 수정**: 최초 적용한 스케일 값(ogro 0.045, elf 0.014)에서 elf가 화면에 거의 안 보이는 문제 발견 → `THREE.Box3().setFromObject()`로 각 모델의 원본 바운딩 박스 크기를 직접 측정(ogro ≈ 87 유닛, elf ≈ 7.25 유닛 — 원본 스케일 단위가 서로 크게 다름)한 뒤, 목표 높이(≈1.2~1.5 월드 유닛)에 맞춰 역산하여 `0.017`(ogro) / `0.165`(elf)로 교정, 재검증 스크린샷으로 둘 다 적절한 크기로 보이는 것 확인
  - 검증 후 디버그 코드는 제거함
- **주의사항**: `elf.dae`는 Z-up 좌표계 에셋이라 `ColladaLoader`가 자동으로 Y-up 회전을 적용한다는 콘솔 경고가 뜨는데, 정상 동작이며 우리 쪽 회전(`root.rotation.y`)과 충돌하지 않음
- **파일**: `index.html`

## Step 16. 최종 보스 크기/방향 수정 (완료)

- **문제**: 최종 보스가 플레이어에게 다가올 때 등을 보이며(뒤돌아서) 다가오고, 크기가 다소 큼
- **수정**: `BOSS_MODEL_SCALE.final`을 1.9 → 1.5로 축소. 보스 회전 공식을 `-atan2(dy,dx) - PI/2`에서 `-atan2(dy,dx) + PI/2`로 변경(180도 보정) — RobotExpressive 모델의 정면 축이 Soldier.glb와 반대라 생긴 문제였음 (중간 보스도 같은 코드를 공유하므로 함께 수정됨)
- **검증 방법**: 디버그 훅으로 최종 보스를 플레이어 위쪽에 배치해 정면(팔·몸통)이 플레이어 쪽(아래)을 향하는지 스크린샷으로 확인
- **파일**: `index.html`

## Step 17. 무기 3D 모델 적용 + 투사체 비주얼 개선 (완료)

- **구현 내용**
  - **무기**: three.js MD2 예제 자산 중 `examples/models/md2/ratamahatta/w_blaster.md2`(공식 예제에서 캐릭터가 손에 드는 블래스터 총 모델)를 `MD2Loader`로 로드해 그대로 사용. 요청하신 대로 **손에 쥔 형태가 아니라** 캐릭터의 진행 방향 옆쪽(측면 벡터)에 살짝 띄운 형태로 배치, 다중 사격 카드 개수(`projectileCount`, 최대 4)만큼 총이 옆으로 나란히 늘어남
  - 발사 시(`game.shotFx`) 무기가 뒤로 살짝 반동하고, 가장 앞쪽 총구 위치에 `THREE.PointLight`로 노란 총구 화염이 잠깐 밝아짐
  - 3D 무기가 준비되면 기존 2D 사각형 총열(폴백)은 자동으로 숨겨짐(`three3d.sceneReady && three3d.weaponMeshes` 체크)
  - **투사체**: 기존의 단순한 원 + 얇은 잔상선을 진행 방향으로 길쭉한 "에너지 볼트"(그라디언트 잔상 꼬리 + 밝은 탄두)로 교체해 훨씬 또렷하게 보이도록 개선
- **검증 방법**
  - 디버그 훅으로 무기 템플릿 로드 확인, `projectileCount=3`으로 강제 설정 후 스크린샷 → 총 3정이 캐릭터 옆에 나란히 뜬 것을 확인
  - 투사체를 강제 생성 후 `getImageData`로 유채색 픽셀 검출 + 스크린샷으로 볼트 모양 확인
  - 콘솔 에러 없음 확인, 검증 후 디버그 코드 제거
- **주의사항**: 중간보스로 요청받은 Sketchfab "Deadpool Monster King" 모델은 (1) 마블 소유 캐릭터라 저작권 문제가 있고 (2) Sketchfab이 코드로 바로 가져올 수 있는 raw 파일 링크를 제공하지 않아(로그인/다운로드 절차 필요) 자동 연동이 불가능해서, 사용자에게 확인 후 "비슷한 CC0 몬스터로 대체" 결정에 따라 기존 RobotExpressive.glb 기반 중간보스를 유지함(별도 교체 없음)
- **파일**: `index.html`

## Step 18. 오크(ogro) 몬스터가 옆을 보며 따라오는 방향 버그 수정 (완료)

- **문제**: Step 15에서 적용한 ogro(MD2) 몬스터가 플레이어를 쫓아올 때 정면이 아니라 옆을 보며 따라옴
- **원인**: MD2 원본 모델은 좌표축 자체가 다른 3D 모델(Soldier.glb, RobotExpressive.glb)과 다른 방향을 "정면"으로 두고 있는데, 이 보정을 빼먹고 다른 모델과 동일한 회전식(`-atan2(dy,dx) - PI/2`)만 그대로 적용했음. three.js 공식 `MD2Character.js`도 MD2 파츠(body/weapon) 메시에 `mesh.rotation.y = -Math.PI/2` 보정을 걸어두는데, 우리는 이 로컬 보정 없이 바로 그룹 회전만 시켰던 것
- **수정**: ogro 메시 생성 시 `mesh.rotation.y = -Math.PI/2`를 한 번 적용(공식 예제와 동일한 보정값). 이후 매 프레임 그룹(`root`) 회전은 기존 로직 그대로 유지
- **검증 방법**: 디버그 훅으로 ogro를 플레이어 바로 위쪽에 배치하고, 눈으로 방향을 더 잘 확인하기 위해 스케일을 임시로 크게 키워(0.017→0.09) 스크린샷 확인 → 정면(팔/가슴)이 플레이어 쪽(아래)을 향하는 것을 확인 후 스케일 값 원복. 콘솔 에러 없음 확인, 디버그 코드 제거
- **주의사항**: 실시간 게임 루프(`updateThree`)는 `game.paused`와 무관하게 매 프레임 실행되므로, 디버그 훅에서 `three3d` 객체의 값을 수동으로 바꿔도 다음 프레임에 바로 덮어써짐 — 이런 값을 확인하려면 소스 코드의 상수 자체를 임시로 바꾼 뒤 페이지를 새로고침해서 확인해야 함
- **파일**: `index.html`

## Step 19. 오크 몬스터 방향 재수정 + 던전 스타일 배경 (완료)

- **오크 방향 재수정**: Step 18에서 건 `mesh.rotation.y = -Math.PI/2` 보정이 이번엔 반대로 등을 보이며 다가오는 문제로 이어짐 → `+Math.PI/2`(180도 추가 보정)로 변경. "이전 값이 정반대였다"는 사용자 피드백을 그대로 반영해 180도를 뒤집은 것이라 수학적으로 확실한 수정
- **던전 배경 교체**: 사용자가 공유한 참고 이미지(돌바닥 + 모서리의 붉은 크리스탈 기둥 + 보랏빛 무드 조명)를 스타일 참고로 재해석해서 적용 (이미지 자체를 텍스처로 박아넣지 않고, 같은 분위기를 3D 지오메트리로 재구성 — 기존에 지켜온 "실제 이미지 재현 대신 자체 도형으로 재해석" 원칙 유지)
  - 바닥 타일 패턴을 보랏빛이 도는 무드 있는 석재 톤(mauve/plum)으로 재배색
  - 아레나 테두리를 따라 돌 블록(curb) 라인 추가
  - 4개 모서리 + 4개 변 중앙, 총 8곳에 어두운 받침대 + 그 위에 붉게 빛나는(emissive) 크리스탈 가시 클러스터 배치, 각 기둥에 붉은 포인트라이트로 은은한 글로우 추가
  - 조명 색감을 흰색 계열에서 보랏빛(HemisphereLight/DirectionalLight 색상 변경)으로 조정해 전체적인 무드를 참고 이미지에 가깝게 맞춤
- **검증 방법**: 브라우저 스크린샷으로 오크가 플레이어 쪽(정면)을 보고 다가오는지, 던전 배경(돌바닥/테두리/기둥)이 참고 이미지와 유사한 분위기로 렌더링되는지 확인. 기존 3D 오브젝트(장애물/적/무기)와 z-order 상 이펙트가 여전히 잘 보이는지도 함께 확인. 콘솔 에러 없음
- **파일**: `index.html`

## Step 20. 효과음 + 배경음악 추가 (완료)

- **구현 내용**: 외부 음원 파일을 다운로드하지 않고(저작권/에셋 탐색 부담 없이) **Web Audio API로 직접 합성**하는 방식으로 구현("만들어서 넣어달라"는 요청에 맞춰 직접 생성)
  - 공용 오디오 그래프: `AudioContext` + `masterGain` → `sfxGain`/`musicGain` 두 버스로 분리
  - `playTone()`: 오실레이터 기반 톤(주파수 스윕 지원), `playNoise()`: 화이트 노이즈 버퍼 + 필터 기반 타격/충격음
  - **효과음**: 공격 발사음(짧은 스퀘어파 "pew"), 피격음(둔탁한 저음+노이즈), 적 처치음(일반/보스 구분되는 톤), 카드 선택음(3음 상승 아르페지오), 보스 등장음(저음 럼블+노이즈), 스테이지 클리어(4음 상승 팡파레), 게임 오버(4음 하강 톤)
  - **배경음악**: 마이너 코드 진행 8스텝 루프를 `setInterval` 스텝 시퀀서로 계속 재생(정밀한 오디오 클럭 스케줄러 대신 단순 타이머 방식 — 하비 프로젝트 규모에 맞춰 단순화, 약간의 타이밍 지터는 허용). 스테이지 시작 시 재생 시작, 메인 메뉴/상점/게임종료 시 정지
  - 브라우저 자동재생 정책 대응: `AudioContext`는 "스테이지 시작" 버튼 클릭(사용자 제스처) 시점에 `ensureAudio()`로 생성/resume하도록 배치
- **검증 방법**: 디버그 훅으로 모든 효과음 함수를 한 번씩 강제 호출해 예외 없이 실행되는지 확인, `audioCtx.state`가 `"running"`으로 전환되는지 확인, BGM 시작/정지/승리·패배 처리까지 이어지는 흐름에서 에러가 없는지 확인. 콘솔 에러 없음 확인 후 디버그 코드 제거
- **주의사항**: 자동화된 사운드 검증은 "소리가 실제로 잘 들리는지"까지는 확인할 수 없는 한계가 있음 — 실제 듣기 좋은 밸런스인지는 사용자가 직접 플레이하며 확인 필요
- **파일**: `index.html`

## Step 21. 사용자 제공 이미지 적용(카드/장애물/배경/타이틀) + 사운드 강화 (완료)

- **에셋 준비**: 사용자가 대화창에 붙여넣은 이미지는 파일 시스템에 저장되지 않으므로, `assets/` 폴더를 만들어 사용자가 직접 파일로 저장하도록 안내함. 저장된 원본 9장 중 4장을 실제로 사용(나머지는 다른 스타일/레이아웃 참고용 목업이라 이번 범위에서는 사용하지 않고 `ref_collage_*.png`, `shop_bg.png`, `permanent_cards.png`로 남겨둠):
  - `assets/background.png` (1448×1086) — 던전 바닥/테두리 전경
  - `assets/cards.png` (1122×1402, 4×4 카드 16종 시트)
  - `assets/obstacles.png` (1536×1024, 3×2 장애물 6종 시트, 흰 배경)
  - `assets/title_bg.png` (941×1330) — 원본 타이틀 이미지에서 "터치하여 시작" 문구와 하단 아이콘 줄을 잘라낸 버전
  - PowerShell(`System.Drawing.Bitmap`)로 각 이미지의 행/열 밝기 프로파일을 스캔해 그리드 경계(카드 16종의 실제 셀 좌표, 장애물 3×2 셀)를 실측한 뒤, 크롭한 테스트 이미지를 직접 눈으로 확인하고 좌표를 확정함(감으로 등분하지 않음)
- **배경**: `getGroundPattern()`(절차적 타일 캔버스)과 `buildDungeonDecor()`(테두리 블록/기둥 절차적 지오메트리)를 완전히 제거하고, 3D 바닥 평면(`THREE.TextureLoader`)과 2D 폴백 모두 `assets/background.png`를 그대로 적용(이미지 안에 테두리/기둥이 이미 포함되어 있어 절차적 장식이 불필요해짐)
- **장애물**: `assets/obstacles.png`는 흰 배경이라, 로드 시 한 번만 6칸으로 잘라 각 칸의 밝은(흰색) 픽셀을 알파 0으로 만드는 크로마키 처리(경계는 부드러운 알파 페이드)를 거쳐 투명 배경 캔버스 6장을 만듦(`buildObstacleSkins()`). 장애물 생성 시(`generateObstacles()`) 6종 중 랜덤 스킨을 배정하고, 3D에서는 기존 팔면체 크리스탈 대신 `THREE.Sprite`(스킨별 재질은 여러 장애물이 공유, `rebuildObstacles3D()`에서 dispose하지 않도록 `userData.shared` 플래그로 구분)로, 2D 폴백은 크로마키 캔버스를 `drawImage`로 그림. 회전은 스프라이트의 `material.rotation`으로 화면 평면상에서 유지(기존 회전 애니메이션 그대로 보임)
- **카드**: `CARD_POOL`의 16개 항목 각각에 실측한 `iconRect:[x,y,w,h]`를 추가하고, `cardIconStyle()`로 목표 아이콘 박스(60px 폭) 기준 `background-size`/`background-position`을 픽셀 단위로 역산해 CSS 스프라이트로 적용. 레벨업 카드 UI(`openLevelUp()`)에서 이미지가 준비되면 이모지 대신 실제 카드 아트를, 로드 실패 시에는 기존 이모지로 자동 대체
- **타이틀/메인 메뉴**: `#menu-overlay`에 `title_bg.png` + 어두운 그라데이션 오버레이를 배경으로 적용하고, 기존 h2 제목(이미지에 로고가 이미 포함되어 중복)은 숨김. 기존 실제 동작하는 버튼(스테이지 시작/상점)은 그대로 유지하고 "터치하여 시작" 문구만 제거(이미지 자체에서 크롭)
- **사운드 강화**: 기존 효과음을 더 어둡게 재조정 — 발사음에 노이즈 크랙 추가, 피격음/보스 등장음/처치음에 살짝 어긋난 저음 두 개를 겹쳐 맥놀이(디소넌스)를 내고, 패배음도 더 느리고 어두운 하강 진행 + 저음 노이즈 럼블로 변경. 신규로 메인 메뉴/상점 전용 앰비언스(살짝 어긋난 두 저음 드론 + 이따금 낮은 삐걱임 노이즈, `startAmbience()`/`stopAmbience()`)와 모든 내비게이션/상점 버튼 공통 클릭 효과음(`sfxClick()`, `guardedHandler()` 안에서 일괄 재생)을 추가
- **검증 방법**: 로컬 서버 브라우저 미리보기로 메인 메뉴(배경 이미지 확인) → 상점(콘솔 에러 없음) → 스테이지 플레이(배경/장애물 스프라이트 렌더링, 크로마키로 흰 배경 안 보이는지) 확인. 카드 아이콘은 `window.__debug = { openLevelUp }`을 임시로 추가해 실제 게임 진행 중 강제로 레벨업 오버레이를 띄워 카드 3장에 실제 아트가 정확한 위치로 잘려 나오는지 스크린샷으로 확인(가시 갑옷/폭발탄/냉기 사격/긴 팔 카드로 각각 확인) 후 디버그 코드 제거. 콘솔 에러 없음, `assets/*.png` 4개 모두 200 OK로 로드됨을 네트워크 탭으로 확인. 사운드는 기존과 동일한 한계로 실제 청감 밸런스까지는 자동 검증 불가 — 사용자가 직접 들어보고 볼륨/느낌 피드백 필요
- **주의사항**: 대화창에 붙여넣은 이미지는 파일로 저장되지 않으므로, 다음에 새 이미지 에셋을 반영해야 할 때도 사용자에게 `assets/` 폴더에 파일로 저장해달라고 먼저 요청해야 함. `obstacles.png`/`cards.png` 원본을 남겨두었으므로 그리드 좌표를 조정해야 하면 원본에서 다시 계산 가능
- **파일**: `index.html`, `assets/background.png`, `assets/cards.png`, `assets/obstacles.png`, `assets/title_bg.png` (+ 참고용 `assets/ref_collage_1~3.png`, `assets/shop_bg.png`, `assets/permanent_cards.png`, 원본 `assets/title.png`)

## Step 22. UI 전면 개편(폰트/버튼/상점) + 장애물 고정 + 드랍 비주얼 + 공포 BGM (완료)

요청 9개 항목을 한 번에 반영. 이번엔 새 에셋을 받지 않고, 이미 `assets/`에 있던 이미지(`shop_bg.png`, `permanent_cards.png`)를 추가로 활용 + CSS로 직접 UI를 제작함.

- **1. 폰트 (게임 분위기)**: Google Fonts에서 굵고 각진 `Black Han Sans`(제목/버튼/HUD 숫자)와 둥근 게임체 `Do Hyeon`(본문)을 불러와 CSS 변수(`--font-title`, `--font-body`)로 전역 적용. 기존 `Malgun Gothic`은 폴백으로 남김
- **2. 기본 UI 재설계**: 어두운 던전 팔레트(보랏빛 석재 `--stone-0~3` + 황금 `--gold` + 불꽃 `--ember`)를 CSS 변수로 정의하고, 모든 패널에 "석재 플레이트" 표현(레이어드 그라디언트 + `inset` 하이라이트/그림자 + 외곽 발광)을 적용. `#app`(보랏빛 발광 테두리), `#hud`(베벨 처리된 미니 플레이트 칩, 금색 수치), 경험치 바(보라 에너지 게이지 + 발광), 화면 제목(금속 각인 그라디언트 텍스트 + 양옆 장식선), 보스 배너(핏빛 경고 플레이트 + 발광 펄스 애니메이션)까지 일괄 교체
- **3·9. 시작 화면 버튼 직접 제작**: 참고 이미지의 버튼 스타일(석재 플레이트 + 얇은 밝은 테두리 + 양옆 다이아 젬 + 눌리는 입체감)을 이미지 크롭이 아니라 **순수 CSS로 자체 구현**한 `.gbtn` 컴포넌트. `::before`/`::after`로 좌우 젬을, `outline-offset:-6px`로 내부 이중 테두리를, `box-shadow`의 하드 오프셋으로 두께감을 만들고 `:active`에서 실제로 눌리게 함. 주 버튼(`.gbtn.primary`)은 불꽃 톤, 보조는 보라 석재 톤, 작은 버전(`.gbtn.small`)까지 3종
- **4. 시작 화면에서 조작법이 메인 아트를 가리던 문제**: 항상 펼쳐져 있던 `.rules` 박스를 `<details>` 기반 접이식(`.howto`)으로 바꿔 **기본 접힌 상태**로 두고, 요약 버튼("❔ 조작법 보기")은 내용 폭이 아니라 글자 폭에 맞게 축소(`align-self:center`)해서 타이틀 로고를 덮지 않게 함. 펼치면 스크롤 가능한 석재 패널로 표시됨. 배경 그라디언트도 상단을 훨씬 밝게(0.05~0.12) 조정해 보스 얼굴/로고가 잘 보이도록 수정
- **5. 상점 UI 전면 변경**: 배경에 `assets/shop_bg.png`(상점 내부)를 적용하되 이미지에 이미 박혀 있는 "상점" 로고와 우리 제목이 겹치지 않도록 `background-position:center 62%`로 내부 카운터 영역이 보이게 조정. 카드는 기존 레벨업 카드와 공유하던 `.lvcard`에서 분리해 `.shopcard`를 신설하고, `assets/permanent_cards.png`의 4개 영구 강화 아트(검/하트/부츠/자석)를 실측 좌표로 잘라 각 항목에 매칭(우리 `SHOP_ITEMS`의 hp/atk/speed/magnet 순서와 이미지 배치 순서가 달라 `artRect`로 개별 지정). 강화 레벨은 숫자 대신 다이아 핍 5개로 표시하고, 구매 버튼은 금색 각인 플레이트로 교체. 골드 부족 시 카드 전체가 채도 낮아지고 버튼이 회색 비활성 상태가 됨
- **6. 장애물 회전 제거**: `generateObstacles()`에서 `angle`/`spin` 필드를 아예 없애고, `update()`의 각도 증가 로직과 `updateThree()`의 회전 동기화, 2D 폴백의 `ctx.rotate()`를 모두 제거. 이제 아트 그대로 바닥에 세워진 고정 구조물로 보임(아이콘이 "세워진 제단" 형태라 회전시키면 어색했던 문제 해결)
- **7. 몬스터 드랍 비주얼**: 초록 원이었던 경험치 구슬을 던전 분위기에 맞는 **보랏빛 마력 결정**으로 교체 — 3중 방사 그라디언트 발광 + 다이아몬드 컷 보디(밝은 라벤더→퍼플→딥바이올렛 그라디언트) + 흰 테두리 + 좌상단 패싯 하이라이트, 그리고 `game.elapsed` 기반으로 크기가 미세하게 맥동. 사정거리 링 색상도 파랑에서 보라로 맞춤
- **8. 공포 BGM + 효과음**: BGM을 단순 마이너 루프에서 **4겹 공포 트랙**으로 재작성 — (a) D1(36.71Hz)과 살짝 어긋난 38.89Hz의 초저음 드론 페어가 계속 맥놀이를 만들고, (b) 한 마디에 두 번 "쿵-쿵" 심장박동, (c) 반음/트라이톤(G#2)으로 기어가는 불협 베이스 16스텝 시퀀스, (d) 이따금 멀리서 들리는 높은 비명 같은 스트링 스탭 + 바람 노이즈. 드론 생성 로직은 `startDrone()`/`stopDrone()` 공용 헬퍼로 묶어 메뉴 앰비언스와 공유하고, 시작 시 1.2초 페이드인으로 갑자기 튀지 않게 함. 효과음도 추가 강화: 피격음에 날카로운 크랙 + 둔탁한 저음 + 어긋난 맥놀이 3겹, 카드 획득음을 밝은 메이저에서 어두운 마이너 아르페지오 + 결정 잔향으로, 승리음도 낮은 종소리 위 마이너 팡파레로 변경. 메뉴 앰비언스에는 물방울 소리 추가
- **검증 방법**
  - 브라우저 미리보기로 메인 메뉴(타이틀 아트 전체가 보이고 조작법이 접혀 있음) → 상점(배경/아트/핍/가격 플레이트/비활성 상태) → 스테이지 클리어 화면 → 실제 플레이(장애물이 고정되어 세워져 있음) 각각 스크린샷 확인
  - 미리보기 탭이 백그라운드일 때 `requestAnimationFrame`이 멈춰 적 스폰이 안 보이는 기존 이슈가 있어, 임시 디버그 훅(`window.__debug = { getGame, draw, update, openLevelUp, sfx, startBgm, ... }`)으로 `game.paused=true` 후 마력 결정 15개를 격자로 강제 배치하고 `draw()`를 직접 호출해 새 드랍 비주얼을 스크린샷으로 확인. 장애물 객체에 `angle`/`spin` 키가 실제로 없어진 것도 같은 훅으로 확인
  - 레벨업 카드는 훅으로 `openLevelUp()`을 강제 호출해 새 카드 스타일(아트 아이콘 + 등급색 테두리/발광) 확인
  - 사운드는 훅으로 효과음 8종 + BGM/앰비언스를 모두 호출해 예외 없이 실행되는지, `audioCtx.state === "running"`인지 확인하고, BGM/앰비언스 시작·정지를 3회 반복해도 오류가 없는지(드론 오실레이터 정리 누락 여부) 확인
  - 검증 후 디버그 훅은 전부 제거(`grep __debug` 0건 확인), 콘솔 에러 없음 확인
- **주의사항**
  - 폰트가 Google Fonts CDN 의존이라 오프라인에서는 `Malgun Gothic` 폴백으로 표시됨(기존 three.js CDN 의존과 같은 성격)
  - 상점 강화 레벨 핍은 5칸까지만 표시되므로, 6단계 이상 강화하면 핍이 5개로 고정된 채 가격만 계속 올라감(현재 밸런스에서는 문제 없지만 상한을 늘리려면 `SHOP_MAX_PIPS` 수정)
  - 자동 검증 과정에서 미리보기 브라우저의 `localStorage` 진행도(골드/강화)가 테스트 구매로 오염되어, 확인 후 `tangtang-meta-v1` 키를 삭제해 초기화해 둠
  - 사운드의 실제 청감(공포 강도/볼륨 밸런스)은 여전히 자동 검증 불가 — 직접 들어보고 조정 필요
- **파일**: `index.html` (+ 기존 `assets/shop_bg.png`, `assets/permanent_cards.png`를 이번에 실제 사용 시작)

## Step 23. 장애물 정리+경기장 고정, 상점 확장, 레벨업 요약, file:// 호환성 (완료)

Step 22 스크린샷을 본 뒤 이어진 5가지 후속 요청을 반영.

- **장애물 흙 제거**: `assets/obstacles.png`는 알파 채널이 있지만 "흙"까지 불투명하게 그려져 있어 단순 크롭으로는 제거가 안 됨(첫 시도: 열별 불투명 높이 임계값 → 여전히 흙 잔여, 두 번째 시도: 국소 표준편차 기반 텍스처 마스킹 → 매끄러운 표면의 오브젝트까지 함께 지워짐). 최종적으로 PowerShell에서 128px 격자+좌표 라벨을 얹은 참고 이미지를 만들어 6개 구조물의 정확한 픽셀 박스를 직접 읽고, 그 박스만 크롭 + 가장자리 알파 페더링(옆 4px, 아래 14px)으로 잘라 `assets/obstacles_cut.png`(3×2, 칸당 256px)를 생성. 흙이 완전히 사라지고 받침대까지만 깔끔하게 남음
- **경기장 범위 실측 + 고정 배치**: `assets/background.png`(1448×1086)를 가로/세로 중앙선 밝기 프로파일로 스캔해 테두리→바닥 전환 지점(x:95~1332, y:130~1036)을 찾고, 900×600 게임 좌표로 환산 + 안쪽 여유를 둔 `ARENA = {x0:66,y0:80,x1:822,y1:560}`을 정의. `generateObstacles()`가 이 범위 안에서만(벽에 파묻히지 않도록 넉넉한 패딩 포함) 5개를 배치
- **이동 범위 제한**: 플레이어는 `clampToArena()`로, 적은 이동 로직 안에서 매 프레임 같은 함수로 경기장 안에 묶어둠(스폰은 경기장 벽 바로 바깥에서 시작해 걸어 들어오되, 첫 프레임부터 클램프되어 벽 경계에서 나타나는 형태). 보스 스폰 위치도 캔버스 중앙이 아니라 경기장 중앙 상단으로 수정
- **게임 중 HUD 버튼**: `#hud` 오른쪽에 "↻ 다시 시작"/"✕ 메인으로" 버튼을 추가(평소엔 숨김, `startNewGame()`에서 표시하고 메뉴/게임종료 시 다시 숨김). 다시 시작은 기존 `retry-btn`과 동일하게 같은 스테이지를 재시작, 메인으로는 진행 중인 `requestAnimationFrame`을 정지시키고 메뉴로 돌아가도록 `showMenu()`를 보강
- **상점 4종 → 8종 확장**: 공격 속도/사정거리/치명타/재생 강화 4종을 추가(각각 `cards.png` 카드 아트에서 아이콘을 재사용). 모든 항목에 `maxLevel`(8~10단계) 상한과 `cur(lv)` 함수(현재까지 누적된 실제 효과 문구, 예: "공격 속도 +8%")를 추가해 카드에 Lv.N/최대단계 진행바 + 현재 효과 + 다음 단계 예고 + MAX 도달 시 구매 비활성화까지 표시. 상점 카드 폭을 줄이고 목록에 세로 스크롤을 둬서 8종이 한 화면에 들어오게 함
- **레벨업 화면 보유 효과 요약**: `game.cardLevels`에 카드별 획득 횟수를 기록하고, 레벨업 화면 하단에 "현재 능력치"(공격력/공격 간격/이동속도/최대체력/사정거리/치명타 요약 한 줄) + "보유 효과" 칩 목록(획득한 카드마다 아이콘+이름+Lv+`readout()`으로 계산한 실제 현재 수치, 예: "동시 조준 2명")을 표시. 제시된 3장의 카드 중 이미 보유한 카드에는 "보유 Lv.N" 배지를 얹어 구분
- **`file://`(직접 파일 열기) 호환성 수정**: 사용자가 "실제 웹으로 키면 적용 안됌"이라고 보고한 원인을 두 가지로 특정함
  1. 이전 버전의 장애물 크로마키 코드가 `getImageData`/`putImageData`로 로컬 이미지의 픽셀을 직접 읽었는데, `file://`에서 로드한 이미지를 그린 캔버스는 브라우저가 "오염된(tainted) 캔버스"로 취급해 픽셀 읽기를 막아 예외가 발생함 → 흙 제거를 오프라인(빌드 타임) 작업으로 옮기고 런타임에는 `drawImage`만 쓰도록 바꿔 근본적으로 해결(위 "장애물 흙 제거" 항목)
  2. 3D 지면(`THREE.TextureLoader`로 `assets/background.png`를 텍스처로 올리던 코드)도 같은 이유로 실패할 수 있어, 던전 배경을 `#field-wrap`의 CSS `background-image`로 완전히 이전(3D 지면 메쉬 자체를 제거) — CSS 배경 이미지는 `file://`에서도 아무 제약 없이 항상 표시됨
  3. 장애물도 3D 텍스처/스프라이트 대신 새로 추가한 `#props` 2D 캔버스에 `drawImage`로 직접 그리도록 변경(레이어 순서: CSS 배경 < `#props`(장애물) < `#three-canvas`(캐릭터) < `#game`(이펙트)). three.js/WebGL이 완전히 실패하는 최악의 경우에도 배경·장애물은 항상 보임
- **검증 방법**: 로컬 서버 브라우저 미리보기로 메인 메뉴(조작법 버튼이 로고를 가리지 않음) → 상점(8종 카드, 스크롤, Lv/MAX/진행바/현재효과 확인) → 실제 플레이(장애물이 흙 없이 깔끔하게 보임, HUD 버튼 노출) 스크린샷 확인. 임시 디버그 훅(`window.__debug`)으로 `game.paused=false` 상태에서 플레이어를 경기장 밖 극단 좌표(-500,-500 / 5000,5000)로 강제 이동시킨 뒤 `update()`를 반복 호출해, 매번 `ARENA` 경계(반지름 보정 포함)로 정확히 클램프되는지 좌표값으로 확인. 레벨업 카드 보유 효과 패널은 `game.cardLevels`를 임의로 세팅한 뒤 `openLevelUp()`을 강제 호출해 스크린샷으로 확인. 검증 후 디버그 훅 제거(`grep __debug` 0건), 콘솔 에러 없음, `localStorage` 테스트 데이터 삭제까지 확인
- **주의사항**: 여전히 Google Fonts와 three.js CDN 모델은 네트워크가 필요하며, `file://`에서 오프라인이면 그 부분만 폴백(기본 폰트, 2D 캐릭터)으로 대체됨 — 이번에 고친 것은 어디까지나 "로컬 `assets/` 이미지가 `file://`에서 아예 안 뜨던" 문제. 실제로 파일 탐색기에서 `index.html`을 더블클릭해 여는 시나리오까지는 이번 세션에서 직접 재현 검증하지 못했고(로컬 서버 미리보기로만 확인), 사용자가 실제 환경에서 다시 확인해 볼 필요가 있음
- **파일**: `index.html`, `assets/obstacles_cut.png`(신규)

## Step 24. 스테이지 난이도 강화, 보스 공격 스킬 + 체력바, 타이틀 BGM, 캐릭터 스킨 (완료)

- **난이도 스케일링 강화**: `stageMultiplier`를 `1+(stage-1)*0.3` → `1+(stage-1)*0.38`로 상향(적/보스 체력·데미지·보상이 스테이지별로 더 가파르게 증가). 기존에는 스폰 간격이 "판 안에서 경과 시간"에만 반응하고 스테이지 번호와는 무관했던 문제를 발견해, `spawnIntervalFor(stage, elapsedMs)`로 교체 — 시작 간격(1400ms)과 최소 간격(450ms) 자체가 스테이지가 오를수록 낮아져(스테이지당 각각 -70ms/-18ms) 후반 스테이지는 시작부터 훨씬 빡빡하게 몰아침
- **보스 공격 스킬**: `idle → telegraph(예고) → active(발동) → recover(재사용대기)` 4단계 상태 머신을 보스 엔티티에 추가(`updateBossSkill`/`startBossSkillTelegraph`/`activateBossSkill`/`updateActiveBossSkill`). 중간 보스와 최종 보스가 서로 다른 스킬 구성을 쓰도록 `BOSS_TYPES[type].skills` 배열로 분리:
  - **중간 보스**: `돌진(dash)`(650ms 예고 후 7.5배속으로 플레이어 방향 돌진, 접촉 시 평소 접촉 피해의 1.8배) / `충격파(nova)`(850ms 예고 후 반경 190까지 확장하는 링이 플레이어를 스쳐 지나갈 때 피해)
  - **최종 보스**: `메테오(meteor)`(1000ms 동안 플레이어 위치에 주황 경고 반경을 표시한 뒤 그 자리에 낙하, 반경 안에 있으면 큰 피해 + 화면 흔들림) / `방사탄(burst)`(500ms 예고 후 10발의 투사체를 사방으로 발사, 신규 `game.enemyProjectiles` 배열 + 전용 충돌 판정으로 처리)
  - 스킬 예고 단계에서는 보스가 제자리에 멈춰 경고 이펙트(펄스 링/조준선/메테오 반경 등)를 보여주고, 3D 보스 모델은 재질 발광(`emissiveIntensity`)이 함께 밝아져 "위험 신호"를 줌. 텔레그래프/임팩트 전용 효과음(`sfxBossChargeUp/Dash/Nova/Impact/Burst`) 신규 추가
  - 돌진 스킬이 실행되는 동안에는 기존 "플레이어 추격 이동"과 "접촉 데미지" 로직이 충돌하지 않도록 별도 분기로 분리하고, 텔레그래프/발동 중에는 보스가 이동하지 않도록(돌진 제외) 잠금
- **보스 체력바 UI**: 화면 상단(보스 등장 배너 바로 아래)에 보스 이름 + 큰 체력 게이지를 표시하는 `#boss-hpbar`를 신규 추가. 매 프레임(`loop()`)에서 살아있는 보스(최종 보스 우선)를 찾아 갱신하고, 보스가 없거나 메뉴/종료 화면에서는 자동으로 숨김
- **시작 화면 전용 BGM**: 기존에는 메인 메뉴/상점이 동일한 저음 앰비언스(드론+노이즈)를 공유했는데, 메인 메뉴에는 별도로 작곡한 `startTitleBgm()`(C2 저음 드론 + 16스텝의 느린 마이너 선율 + 저음 펄스)을 적용해 "예고편"같은 웅장한 느낌을 주고, 상점은 기존의 차분한 앰비언스를 그대로 유지해 두 화면의 분위기를 구분함
- **캐릭터 스킨(상점)**: 상점에 "영구 강화"/"스킨" 탭을 추가하고, 5종 스킨(기본/크림슨 나이트/황금 용병/그림자 추적자/맹독 처형인)을 구매 후 장착할 수 있게 함. 스킨은 별도 3D 모델을 만드는 대신 기존 플레이어 모델(Soldier.glb) 재질에 색을 곱하는 방식(보스 재색칠과 같은 기법)으로 구현 — 기본 스킨은 흰색(무보정)이라 원본 텍스처가 그대로 보임. `meta.skins`(보유 목록)/`meta.equippedSkin`을 `localStorage`에 저장하고, 3D 모델이 아직 로드되지 않았을 때를 위해 2D 폴백 캐릭터 색상(`playerFallbackColor`)도 함께 갱신
- **검증 방법**: `game.running=false`로 자동 루프를 멈춘 뒤 디버그 훅(`window.__debug`)으로 `update()`를 결정론적으로 반복 호출해 4종 스킬을 각각 검증 — 각 스킬 발동 시 정확한 피해량(예: 노바 18×배율, 메테오 24×배율)만큼 플레이어 체력이 줄어드는지, 상태가 `idle→telegraph→active→recover`로 올바르게 순환하는지 좌표/상태값으로 확인. 보스 체력바는 강제로 체력을 42%로 설정한 뒤 스크린샷으로 게이지 비율 확인. 스킨은 골드를 지급해 구매→장착 흐름을 실제 클릭으로 수행하고 `localStorage` 저장값과 3D 모델 색상(스크린샷)까지 확인. 전체 화면(메뉴/상점 강화·스킨 탭/실제 플레이) 스크린샷과 콘솔 에러 없음을 최종 확인 후 디버그 훅 제거, `localStorage` 테스트 데이터 삭제
- **주의사항**: 자동화 도구로 여러 차례 게임을 조작하는 동안 실제 게임 루프가 백그라운드에서 계속 진행되어(rAF는 페이지가 보이는 한 계속 실행됨) 테스트 중인 플레이어 체력이 실제 몬스터에게 깎이는 간섭이 반복적으로 발생했음 — `game.running=false`로 자동 루프 자체를 멈추고 수동으로만 `update()`를 호출해야 완전히 결정론적으로 검증할 수 있었음(이 방식을 이번 세션의 새로운 검증 표준으로 정리해 메모리에도 남겨둠)
- **파일**: `index.html`

## Step 25. 폭발탄 카드 임팩트 개선 + 최종 보스 상향 (완료)

- **폭발탄(스플래시) 임팩트 강화**: 기존에는 명중 지점에 작은 파티클 10개만 튀고 끝나서 "무슨 효과가 일어났는지" 알아보기 어려웠음(사용자 피드백: "효과랑 어떻게 쓰는지 보이지가 않는다"). 다음을 추가:
  - `game.explosions` 배열 신규 추가 — 명중 지점에서 카드의 실제 `splashRadius`만큼 확장하는 이중 링(굵은 반투명 화염색 + 얇은 밝은 노란색)을 그려 **폭발 범위 자체가 눈에 보이게** 함
  - 화면 흔들림(`shakeTime`/`shakeMag`)과 전용 폭발음(`sfxExplosion`: 저음 노이즈+사와톱 임팩트+짧은 파열음)을 추가해 타격감 강화
  - 중심에는 밝은 불꽃色(`#fff3c4`) 파티클 + 주황/적색 파편 파티클을 겹쳐 그려 "폭발" 느낌을 진하게 하고, 스플래시에 맞은 각 적에게는 개별적으로 작은 파티클과 `"-8"` 형태의 피해 숫자(기존 흡혈 카드와 같은 스타일)를 띄워 "이 적도 맞았다"는 걸 하나씩 확인할 수 있게 함
  - 카드 설명/보유효과 문구도 구체적으로 수정: "화살이 명중하면 반경 50 안의 주변 적에게 폭발 피해 8 (충격파로 표시됨)", 레벨업 화면 보유효과는 "폭발 반경 50 · 피해 8"처럼 반경까지 표시
- **최종 보스 상향**(사용자 피드백: "너무 쉽게 잡힌다"): 체력 950→1450(+53%), 접촉 데미지 22→28, 이동속도 50→58, 스킬 재사용 대기시간 4200→3300ms(스킬을 훨씬 자주 씀), 메테오 피해 24→32×배율, 방사탄 10발→12발+피해 9→11×배율로 상향
- **검증 방법**: `game.running=false`로 자동 루프를 멈춘 뒤 디버그 훅으로 스플래시 반경 안/밖에 적을 배치하고 `update()`를 호출해 반경 안의 적만 정확히 `splashDmg`만큼 깎이고 반경 밖의 적은 그대로인지 좌표/HP 값으로 확인(직접 맞은 적 포함 총 3마리 배치 → 반경 안 2마리만 정확히 반응). `game.explosions` 객체가 명중 지점·정확한 반경으로 생성되는지, `shakeTime`이 갱신되는지 확인. 최종 보스 스탯은 `BOSS_TYPES.final`을 디버그 훅으로 직접 읽어 의도한 수치로 반영됐는지 확인. 콘솔 에러 없음 확인 후 디버그 훅 제거
- **주의사항**: 폭발 링 애니메이션은 340ms에 걸쳐 서서히 커지므로, 명중 직후 첫 프레임에는 반경이 거의 0이라 잘 안 보임 — 자동화 스크린샷으로 확인할 때는 몇 프레임 지난 뒤에 캡처해야 링이 눈에 보임(실제 플레이에서는 자연스럽게 애니메이션되므로 문제 없음)
- **파일**: `index.html`

## Step 26. 폭발탄 화면 흔들림 제거 (완료)

- **문제**: Step 25에서 폭발탄 스플래시 임팩트에 화면 흔들림(`game.shakeTime`/`shakeMag`)을 추가했는데, 스플래시는 "화살이 명중할 때마다"(카드 1회 획득으로 얻는 지속 효과) 매번 발동하는 구조라서, 공격 속도가 빠르거나 다중 사격/관통 카드와 겹치면 200ms 흔들림이 그보다 짧은 간격으로 계속 재발동되어 화면이 거의 끊이지 않고 흔들리는 문제가 있었음(사용자 피드백)
- **수정**: 스플래시 히트 처리에서 `game.shakeTime`/`game.shakeMag` 갱신 두 줄만 제거. 충격파 링, 파티클, 폭발음, 개별 "-N" 피해 숫자는 그대로 유지해 임팩트 자체는 계속 잘 보임
- **검증 방법**: 디버그 훅으로 `game.player.atkInterval`을 30ms로 극단적으로 낮추고 스플래시가 걸린 적을 8회 연속 명중시켜 `game.shakeTime`이 매번 0으로 유지되는지(더 이상 갱신되지 않는지) 확인. 콘솔 에러 없음 확인 후 디버그 훅 제거
- **참고**: 화면 흔들림 자체는 다른 곳(플레이어가 피격당할 때, 보스의 돌진/메테오 스킬 명중 시, 여벌 목숨 부활 시)에는 여전히 남아있음 — 이런 경우들은 사건 자체가 빈번하지 않아 문제가 없고, 사용자도 폭발탄 스플래시만 콕 집어 문제를 제기했으므로 그 부분만 수정함
- **파일**: `index.html`

## 남은 작업

- 없음 (요청하신 항목 모두 반영 완료 — 단, `file://` 직접 열기 수정은 사용자 환경에서의 재확인 필요)
